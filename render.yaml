services:
  - type: web
    name: tribastion-api
    env: docker
    plan: free
    branch: main
    dockerCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        value: https://tribastion.vercel.app
